general:
  artifacts:
    - packages

machine:
  services:
    - docker

dependencies:
  pre:
    - mkdir packages
    - echo -e "$RSA_PUBLIC_KEY" > packages/sgerrand.rsa.pub
  override:
    - docker run -e RSA_PRIVATE_KEY="$RSA_PRIVATE_KEY" -e RSA_PRIVATE_KEY_NAME="sgerrand.rsa" -v $(pwd):/home/builder/package -v $(pwd)/packages:/home/builder/packages sgerrand/alpine-abuild

test:
  override:
    - docker run -it -v $(pwd)/packages:/packages alpine:3.3 sh -c "cp /packages/sgerrand.rsa.pub /etc/apk/keys/ && apk add --allow-untrusted --no-progress --update-cache --upgrade /packages/builder/x86_64/*.apk"
